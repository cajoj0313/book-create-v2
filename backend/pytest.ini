[pytest]
# pytest 配置文件
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 标记配置
markers =
    integration: 集成测试（需要真实 API）
    slow: 慢速测试

# 输出配置
addopts = -v --tb=short